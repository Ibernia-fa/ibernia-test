users.json
lifecycle-users.json
lifecycle-users-*.json
identity-user-sub-map.json
# k6 SIGNUP_BAND_STATE=persistent runtime state (optional; recreated when used)
k6-signup-band-state.json
k6-events-custom-summary.json

# Volume scenario manifests with live IDs (regenerated by Phase A merge)
data/scenarios/profile_*.json
data/scenarios/fixed-advisors-dev-20.json

# User pool (SQLite + leased slices + per-run worker copies)
data/user-pool/**/*.db
data/user-pool/**/pool-slice-*.json
data/user-pool/**/user-slices-*/
data/user-pool/**/exports/
tools/pool-cli/node_modules/

# Per-module k6 handleSummary / adaptive reports (regenerated each run)
k6/**/reports/
reports/*-adaptive-latency-*

# Phase A volume runs (regenerated; keep merged fleet outputs only in git if committed)
reports/phase-a/*/workers/
reports/phase-a/*/logs/
reports/journeys/*.json
reports/journeys/*.md
!reports/journeys/.gitkeep

# Consolidated perf (generated; keep docs/samples/)
reports/consolidated/runs/
reports/consolidated/logs/
reports/consolidated/spill/
reports/consolidated/slices/
reports/consolidated/_debug-*.json
reports/consolidated/consolidated-api-performance.json
reports/consolidated/consolidated-api-performance.md
reports/consolidated/consolidated-api-performance.pdf

# Root npm install (md-to-pdf)
node_modules/
